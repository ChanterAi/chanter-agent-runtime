{
  "name": "chanter-agent-runtime",
  "version": "0.1.0",
  "description": "Shared typed agent task-lifecycle runtime (Goal -> Plan -> Task -> Approval Gate -> Execution -> Evidence -> Validation -> Review -> Result -> Next Recommendation) for CHANTER control products.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node scripts/run-tests.mjs",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "chanter",
    "agent-runtime",
    "orchestration",
    "control-layer"
  ],
  "author": "CHANTER",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
